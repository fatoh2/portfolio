# SOLitaire App Screenshots

Captured on 2026-08-28 from the user-provided local app at
`http://localhost:8093/game` using the browser's viewport screenshot capture.

| Public asset | Screen | Viewport |
| --- | --- | --- |
| `solitaire-lobby-desktop.jpg` | App lobby | 1280 x 800 |
| `solitaire-lobby-mobile.jpg` | Responsive app lobby | 430 x 800 |
| `solitaire-table-mobile.jpg` | Solo Klondike table | 430 x 800 |

Assets live in `public/labs/solitaire/`. They are unaltered JPEG captures of
the rendered app, not generated marketing artwork. No connected wallet,
account details, admin screens, or payment transactions were captured.
These screenshots document the local build, not a verification that the
redesign has shipped to the SOLitaire public website.

The solo table was opened with Play Solitaire > Start Solo Table. No paid
match or wallet connection was used. Wait for the layout and images to finish
loading after each viewport change before taking replacement screenshots.

`solitaireMedia` in `src/content/portfolio.ts` is the shared source for the
homepage, case study, and hero gameplay image. Keep full UI screenshots in
contain mode so navigation and game controls are not cropped. The portfolio's
public product link remains `https://sol-solitaire.com`; localhost is only a
capture source, never a visitor-facing link.
